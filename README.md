# Lead Panel

## Quick Start

```bash
npm run dev
```

## Notes

- Static hosting (e.g., GitHub Pages). No Next.js API routes.
- Supabase REST calls are client-side via `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
